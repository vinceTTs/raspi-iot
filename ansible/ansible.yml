---
- name: Install Docker and Docker-Compose on Raspberry Pi 3
  hosts: all
  become: yes
  vars:
    influx_admin_token: "{{ lookup('env','INFLUX_ADMIN_TOKEN') | default('raspi-admin-token', true) }}"

  tasks:
    - name: Update apt cache and upgrade system
      apt:
        update_cache: yes
        cache_valid_time: 3600
        upgrade: safe

    - name: Install Docker and Docker-Compose
      apt:
        name:
          - docker.io
          - docker-compose
        state: present

    - name: Start and enable Docker service
      systemd:
        name: docker
        state: started
        enabled: yes

    - name: Add current user to docker group
      user:
        name: "{{ ansible_user_id }}"
        groups: docker
        append: yes

    - name: Get current user's home directory
      shell: echo $HOME
      register: user_home
      become: no
      changed_when: false

    - name: Create certificates directory
      file:
        path: "{{ user_home.stdout }}/raspi-iot/certs"
        state: directory
        mode: '0755'
        owner: "{{ ansible_user_id }}"
        group: "{{ ansible_user_id }}"

    - name: Check if CA certificate exists
      stat:
        path: "{{ user_home.stdout }}/raspi-iot/certs/ca.crt"
      register: ca_cert

    - name: Create service directories
      file:
        path: "{{ user_home.stdout }}/raspi-iot/{{ item }}"
        state: directory
        mode: '0777'
      loop:
        - node-red
        - grafana
        - influxdb
        - mosquitto/data
        - mosquitto/log

    - name: Generate CA key and certificate
      shell: |
        openssl genrsa -out ca.key 2048
        openssl req -new -x509 -key ca.key -out ca.crt -subj "/CN=Mosquitto CA"
      when: not ca_cert.stat.exists
      args:
        chdir: "{{ user_home.stdout }}/raspi-iot/certs"
      become_user: "{{ ansible_user_id }}"

    - name: Generate server certificate
      shell: |
        openssl genrsa -out server.key 2048
        openssl req -new -key server.key -out server.csr -subj "/CN=raspi.local"
        openssl x509 -req -in server.csr -CA ca.crt -CAkey ca.key -CAcreateserial -out server.crt -days 3650 -sha256
      when: not ca_cert.stat.exists
      args:
        chdir: "{{ user_home.stdout }}/raspi-iot/certs"
      become_user: "{{ ansible_user_id }}"

    - name: Set certificate permissions
      file:
        path: "{{ user_home.stdout }}/raspi-iot/certs"
        recurse: yes
        mode: '0644'
      when: not ca_cert.stat.exists
      become_user: "{{ ansible_user_id }}"

    - name: Create systemd service for docker-compose
      copy:
        dest: /etc/systemd/system/docker-compose-app.service
        content: |
          [Unit]
          Description=Docker Compose Application Service
          Requires=docker.service
          After=docker.service

          [Service]
          Type=oneshot
          RemainAfterExit=yes
          WorkingDirectory={{ user_home.stdout }}/raspi-iot
          ExecStart=/usr/bin/docker-compose up -d
          ExecStop=/usr/bin/docker-compose down
          TimeoutStartSec=0

          [Install]
          WantedBy=multi-user.target
        mode: '0644'

    - name: Enable and start docker-compose service
      systemd:
        name: docker-compose-app
        enabled: yes
        daemon_reload: yes

    - name: Start InfluxDB only
      shell: |
        cd {{ user_home.stdout }}/raspi-iot
        docker-compose up -d influxdb
      become_user: "{{ ansible_user_id }}"
      changed_when: false

    - name: Wait for InfluxDB CLI ping to succeed
      shell: |
        docker exec influxdb influx ping --host http://localhost:8086
      args:
        chdir: "{{ user_home.stdout }}/raspi-iot"
      register: influx_ping
      retries: 30
      delay: 2
      until: influx_ping.rc == 0
      changed_when: false
      become_user: "{{ ansible_user_id }}"

    - name: Validate admin token can list authorizations and write output
      shell: |
        docker exec -e INFLUX_TOKEN={{ influx_admin_token }} influxdb sh -lc '
          influx auth list --org home --json > /var/lib/influxdb2/auth-list.json 2>&1
        '
      args:
        chdir: "{{ user_home.stdout }}/raspi-iot"
      register: auth_list_check
      changed_when: false
      failed_when: auth_list_check.rc != 0


    - name: Create Grafana read token and Telegraf write token (debuggable)
      shell: |
        docker exec -e INFLUX_TOKEN={{ influx_admin_token }} influxdb sh -lc '
          create_or_get() {
            DESC="$1"; PERM="$2"; FILE="$3";

            echo "=== Creating token: $DESC ===" >&2

            J=$(influx auth create -o home $PERM -d "$DESC" --json 2>/dev/null || true)
            echo -n "$J" > "$FILE.json"

            echo "RAW_JSON_$DESC: $J" >&2

            TOK=$(echo "$J" | jq -r '.token'")

            if [ -z "$TOK" ]; then
              echo "Token not created, checking existing…" >&2
              L=$(influx auth list --org home --json | tr -d "\n")
              echo "LIST_JSON_$DESC: $L" >&2
              TOK=$(echo "$L" | sed -n "s/.*\"description\":\"$DESC\"[^}]*\"token\":\"\([^\"]\+\)\".*/\1/p")
            fi

            echo "FINAL_TOKEN_$DESC: $TOK" >&2

            echo -n "$TOK" > "$FILE"
          }

          create_or_get grafana-read "--read-buckets" /var/lib/influxdb2/grafana-read.token
          create_or_get telegraf-write "--write-buckets" /var/lib/influxdb2/telegraf-write.token
        '
      register: token_creation_output
      args:
        chdir: "{{ user_home.stdout }}/raspi-iot"
      become_user: "{{ ansible_user_id }}"

    - name: Slurp InfluxDB tokens
      slurp:
        src: "{{ user_home.stdout }}/raspi-iot/influxdb/{{ item }}"
      loop:
        - grafana-read.token
        - telegraf-write.token
      register: slurped_tokens

    - name: Fail if any token is empty
      fail:
        msg: "Token {{ item.item }} is empty. Check admin token or Influx CLI permissions."
      when: (item.content | b64decode | trim) | length == 0
      loop: "{{ slurped_tokens.results }}"

    - name: Extract decoded tokens
      set_fact:
        grafana_token: "{{ slurped_tokens.results[0].content | b64decode | trim }}"
        telegraf_token: "{{ slurped_tokens.results[1].content | b64decode | trim }}"

    - name: Update Grafana datasource token
      lineinfile:
        path: "{{ user_home.stdout }}/raspi-iot/config/grafana-datasource-influxdb.yaml"
        regexp: '^\s*token:\s*.*$'
        line: "      token: {{ grafana_token }}"

    - name: Write project .env with Telegraf token
      copy:
        dest: "{{ user_home.stdout }}/raspi-iot/.env"
        content: "TELEGRAF_INFLUX_TOKEN={{ telegraf_token }}\n"
        mode: '0600'

    - name: Start remaining services
      shell: |
        cd {{ user_home.stdout }}/raspi-iot
        docker-compose up -d
      become_user: "{{ ansible_user_id }}"

    - name: Display success message
      debug:
        msg: "Docker and Docker-Compose installed successfully! Docker-compose will start automatically on boot. Please re-login for group changes to take effect."
